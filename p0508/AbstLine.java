package p0508;
/*
 * AbstLine 클래스는 직선을 나타내는 추상 클래스이다.
 * 이클래스는 도형을 나타내는 추상 클래스 Shape로 부터 파생되는 클래스이다.
 * 추상 클래스이므로 본 클래스의 인스턴스를 생성할 수 없다.
 * 구체적인 직선 클래스는 이 클래스를 통해 파생된다.
 * @author 시바타
 * @see Shape
 * @see HozeLine VertLine
 
public class AbstLine extends Shape {

 * 직선의 길이를 나타내는 int형 필드
 
	private int length;
	/*
	 * 직선을 생성하는 생성자이다.
	 * 길이를 인수로 받는다
	 * @param length 생성할 직선의 길이
	 */
/*	public AbstLine(int length) {
		setLength(length);
	}
	/*
	 * 직성의 길이를 반환한다.
	 * @return 직선의 길이
	 
	public int getLength() {
		return length;
	}
	
	/*
	 * 직선의 길이를 설정한다
	 
	public void
}
*/

